---
- hosts:
    - debian
  tasks:
    - name: "Upgrade all installed packages"
      ansible.builtin.apt:
        autoclean: true
        update_cache: true
        cache_valid_time: 21600
        autoremove: true
        only_upgrade: true
        state: latest
        upgrade: safe
      become: true
